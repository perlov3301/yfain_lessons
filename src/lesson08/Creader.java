package lesson08;

public class Creader {
   // the class is meant for reading character streams
	// open a stream(that points at data source)->
	// read data from this stream
	// close the stream unless it's auto-closable
}
